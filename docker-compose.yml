services:
  weather:
    image: ${REPOSITORY_URL}:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weather-alerts
    env_file: .env
    volumes:
      - ./data:/data
    restart: unless-stopped
